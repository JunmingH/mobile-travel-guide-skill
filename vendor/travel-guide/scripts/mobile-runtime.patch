--- a/trip-mode.js
+++ b/trip-mode.js
@@ -1,3 +1,17 @@
+/* Shared mobile helpers: durable commits, explicit failures, user-controlled export. */
+(function(){
+ function openStore(name,store){return new Promise(function(resolve,reject){var done=false,r,timer=setTimeout(function(){finish(new Error('本机数据库暂时无法打开，请关闭其他手册窗口后重试。'))},8000);function finish(error,db){if(done){if(db)db.close();return}done=true;clearTimeout(timer);if(error)reject(error);else{db.onversionchange=function(){db.close()};resolve(db)}}try{r=indexedDB.open(name,1);r.onupgradeneeded=function(){if(!r.result.objectStoreNames.contains(store))r.result.createObjectStore(store,{keyPath:'id'})};r.onsuccess=function(){finish(null,r.result)};r.onerror=function(){finish(r.error||new Error('本机存储不可用'))};r.onblocked=function(){finish(new Error('数据库被另一窗口占用，请关闭其他手册窗口后重试。'))}}catch(e){finish(e)}})}
+ function operation(name,store,method,value){return openStore(name,store).then(function(db){return new Promise(function(resolve,reject){var tx,result;try{tx=db.transaction(store,method==='get'||method==='getAll'?'readonly':'readwrite');var request=tx.objectStore(store)[method](value);request.onsuccess=function(){result=request.result};tx.oncomplete=function(){db.close();resolve(result)};tx.onabort=tx.onerror=function(){var error=tx.error||request.error||new Error('本机保存未完成');db.close();reject(error)}}catch(e){db.close();reject(e)}})})}
+ function sheet(title){var box=document.createElement('div');box.className='mobile-runtime-sheet';box.setAttribute('role','dialog');box.setAttribute('aria-modal','true');box.setAttribute('aria-label',title);box.style.cssText='position:fixed;inset:0;z-index:10000;background:rgba(0,0,0,.45);display:grid;place-items:center;padding:16px';var panel=document.createElement('section');panel.style.cssText='background:#fffdf8;color:#243e35;width:min(600px,100%);max-height:85vh;max-height:85dvh;overflow:auto;padding:18px;border-radius:18px;box-sizing:border-box';var h=document.createElement('h3');h.textContent=title;var close=document.createElement('button');close.type='button';close.textContent='关闭';close.style.cssText='min-height:44px;min-width:64px';var previous=document.activeElement;function finish(){box.remove();if(box.cleanup)box.cleanup();previous&&previous.focus&&previous.focus()}close.onclick=finish;box.onclick=function(e){if(e.target===box)finish()};box.addEventListener('keydown',function(e){if(e.key==='Escape'){e.stopPropagation();finish()}});panel.append(h,close);box.append(panel);document.body.append(box);close.focus();return{box:box,panel:panel}}
+ function manualCopy(value){var ui=sheet('复制完整文字'),p=document.createElement('p'),area=document.createElement('textarea'),select=document.createElement('button');p.textContent='自动复制未完成。点“全选文字”后，使用系统菜单复制。';area.value=value;area.readOnly=true;area.setAttribute('aria-label','完整申请文字');area.style.cssText='display:block;width:100%;min-height:240px;box-sizing:border-box;font-size:16px;line-height:1.5';select.type='button';select.textContent='全选文字';select.style.minHeight='44px';select.onclick=function(){area.focus();area.select();area.setSelectionRange(0,area.value.length)};ui.panel.append(p,area,select);select.click();return false}
+ function legacyCopy(value){var area=document.createElement('textarea');area.value=value;area.readOnly=true;area.style.cssText='position:fixed;top:0;left:0;opacity:.01;font-size:16px';document.body.append(area);var copied=false;try{area.focus();area.select();area.setSelectionRange(0,value.length);copied=document.execCommand('copy')}catch(e){}area.remove();return copied}
+ function copyText(value){var attempt;try{attempt=navigator.clipboard&&window.isSecureContext?navigator.clipboard.writeText(value):Promise.reject(new Error('clipboard unavailable'))}catch(e){attempt=Promise.reject(e)}return Promise.resolve(attempt).then(function(){return true},function(){return legacyCopy(value)||manualCopy(value)})}
+ function fileControls(panel,blob,name){var url=URL.createObjectURL(blob),link=document.createElement('a'),share=document.createElement('button'),status=document.createElement('p');link.href=url;link.download=name;link.textContent='保存文件';link.style.cssText='display:inline-block;padding:12px';panel.append(link);var file=typeof File==='function'?new File([blob],name,{type:blob.type}):null;if(file&&navigator.share&&navigator.canShare&&navigator.canShare({files:[file]})){share.type='button';share.textContent='系统分享 / 保存到文件';share.style.minHeight='44px';share.onclick=function(){navigator.share({files:[file],title:name}).then(function(){status.textContent='系统操作已完成。'},function(e){status.textContent=e.name==='AbortError'?'已取消，文件仍可保存。':'系统分享未完成，请使用“保存文件”。'})};panel.append(share)}status.textContent='请选择保存位置；手册不会代替系统确认文件已保存。';panel.append(status);return{url:url,cleanup:function(){URL.revokeObjectURL(url)}}}
+ function offerFile(blob,name){var ui=sheet('保存完整申请包'),file=fileControls(ui.panel,blob,name);ui.box.cleanup=file.cleanup;return ui}
+ function fromData(data){var parts=String(data).split(','),match=parts[0].match(/^data:([^;]+)/),raw=/;base64/.test(parts[0])?atob(parts.slice(1).join(',')):decodeURIComponent(parts.slice(1).join(',')),bytes=new Uint8Array(raw.length);for(var i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);return new Blob([bytes],{type:match?match[1]:'application/octet-stream'})}
+ function uid(){return crypto&&typeof crypto.randomUUID==='function'?crypto.randomUUID():'local-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2)}
+ window.TravelMobileRuntime={openStore:openStore,operation:operation,copyText:copyText,sheet:sheet,fileControls:fileControls,offerFile:offerFile,fromData:fromData,uid:uid};
+})();
 (function(){
 'use strict';
 var TRIP_MODE_DATA=window.BALI_ITINERARY||[];
@@ -24,17 +38,17 @@
 function backupPlan(d){var value=String(d.fallback||'').trim();if(!value)return'';return '<details class="trip-shopping-tip trip-backup-tip"><summary><span><small>PLAN B · 天气与体力</small><b>备选方案</b></span><i>＋</i></summary><div class="trip-shopping-body">'+value.split(/\n+/).filter(function(t){return t.trim()}).map(function(t){return '<p>'+esc(t)+'</p>'}).join('')+'</div></details>'}
 var DB='@@NAMESPACE@@-reference-photos',STORE='day-reference-photos';
 function photoKey(day){return'trip-photo-day:'+day}
-function openDb(){return new Promise(function(resolve,reject){var r=indexedDB.open(DB,1);r.onupgradeneeded=function(){if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:'id'})};r.onsuccess=function(){resolve(r.result)};r.onerror=function(){reject(r.error)}})}
-function records(day){if(window.CloudTicketVault)return window.CloudTicketVault.all(photoKey(day)).then(function(items){return items.map(function(x){return{id:x.id,day:day,name:x.fileName||x.title,dataUrl:x.dataUrl,createdAt:x.createdAt,cloud:true}}).sort(function(a,b){return a.createdAt-b.createdAt})});return openDb().then(function(db){return new Promise(function(resolve,reject){var r=db.transaction(STORE).objectStore(STORE).getAll();r.onsuccess=function(){resolve(r.result.filter(function(x){return x.day===day}).sort(function(a,b){return a.createdAt-b.createdAt}))};r.onerror=function(){reject(r.error)}})})}
-function put(x){if(window.CloudTicketVault)return window.CloudTicketVault.put({id:x.id,placeKey:photoKey(x.day),title:x.name||'参考样片',fileName:x.name||'参考样片.jpg',dataUrl:x.dataUrl,createdAt:x.createdAt});return openDb().then(function(db){return new Promise(function(resolve,reject){var r=db.transaction(STORE,'readwrite').objectStore(STORE).put(x);r.onsuccess=resolve;r.onerror=function(){reject(r.error)}})})}function del(id){if(window.CloudTicketVault)return window.CloudTicketVault.delete(id);return openDb().then(function(db){return new Promise(function(resolve,reject){var r=db.transaction(STORE,'readwrite').objectStore(STORE).delete(id);r.onsuccess=resolve;r.onerror=function(){reject(r.error)}})})}
+function openDb(){return window.TravelMobileRuntime.openStore(DB,STORE)}
+function records(day){if(window.CloudTicketVault)return window.CloudTicketVault.all(photoKey(day)).then(function(items){return items.map(function(x){return{id:x.id,day:day,name:x.fileName||x.title,dataUrl:x.dataUrl,createdAt:x.createdAt,cloud:true}}).sort(function(a,b){return a.createdAt-b.createdAt})});return window.TravelMobileRuntime.operation(DB,STORE,'getAll').then(function(items){return items.filter(function(x){return x.day===day}).sort(function(a,b){return a.createdAt-b.createdAt})})}
+function put(x){if(window.CloudTicketVault)return window.CloudTicketVault.put({id:x.id,placeKey:photoKey(x.day),title:x.name||'参考样片',fileName:x.name||'参考样片.jpg',dataUrl:x.dataUrl,createdAt:x.createdAt});return window.TravelMobileRuntime.operation(DB,STORE,'put',x)}function del(id){if(window.CloudTicketVault)return window.CloudTicketVault.delete(id);return window.TravelMobileRuntime.operation(DB,STORE,'delete',id)}
 function fileData(f){return new Promise(function(resolve,reject){var r=new FileReader();r.onload=function(){resolve(r.result)};r.onerror=function(){reject(r.error)};r.readAsDataURL(f)})}
 function photoRecord(r){return'<figure class="trip-uploaded-photo" data-photo-id="'+esc(r.id)+'"><img src="'+esc(r.dataUrl)+'" alt="'+esc(r.name||'参考样片')+'" data-view-photo><button type="button" data-delete-photo aria-label="删除样片">×</button><span>点击查看大图</span></figure>'}
-function renderPhotos(c,day){var g=$('.trip-uploaded-grid',c),count=$('.trip-photo-count',c),clear=$('[data-clear-photos]',c);return records(day).then(function(items){g.innerHTML=items.length?items.map(photoRecord).join(''):'<p class="trip-upload-empty">还没有样片，可上传构图参考、穿搭或截图。</p>';count.textContent=items.length+' 张样片';clear.disabled=!items.length;$$('[data-view-photo]',g).forEach(function(img){img.onclick=function(){showViewer(img.src,img.alt)}});$$('[data-delete-photo]',g).forEach(function(b){b.onclick=function(){del(b.parentNode.dataset.photoId).then(function(){renderPhotos(c,day)})}})}).catch(function(){g.innerHTML='<p class="trip-upload-empty">样片暂时无法读取，请稍后重试。</p>'})}
-function bindUpload(c,day){var input=$('[data-trip-photo-input]',c),clear=$('[data-clear-photos]',c);input.onchange=function(){var files=Array.prototype.slice.call(input.files||[]).filter(function(f){return/^image\//.test(f.type)}).slice(0,6);Promise.all(files.map(function(f,i){return fileData(f).then(function(dataUrl){return put({id:'trip-photo-'+Date.now()+'-'+i+'-'+Math.random().toString(36).slice(2,7),day:day,name:f.name,dataUrl:dataUrl,createdAt:Date.now()+i})})})).then(function(){input.value='';return renderPhotos(c,day)}).then(function(){toast(window.CloudTicketVault?'样片已上传并共享':'样片已保存到本机')}).catch(function(){toast('样片保存失败')})};clear.onclick=function(){records(day).then(function(items){return Promise.all(items.map(function(x){return del(x.id)}))}).then(function(){renderPhotos(c,day)})}}
+function renderPhotos(c,day){var g=$('.trip-uploaded-grid',c),count=$('.trip-photo-count',c),clear=$('[data-clear-photos]',c);return records(day).then(function(items){g.innerHTML=items.length?items.map(photoRecord).join(''):'<p class="trip-upload-empty">还没有样片，可上传构图参考、穿搭或截图。</p>';count.textContent=items.length+' 张样片';clear.disabled=!items.length;$$('[data-view-photo]',g).forEach(function(img){img.onclick=function(){showViewer(img.src,img.alt)}});$$('[data-delete-photo]',g).forEach(function(b){b.onclick=function(){del(b.parentNode.dataset.photoId).then(function(){renderPhotos(c,day)}).catch(function(){toast('样片删除未完成，请重试。')})}})}).catch(function(){g.innerHTML='<p class="trip-upload-empty">样片暂时无法读取，请稍后重试。</p>'})}
+function bindUpload(c,day){var input=$('[data-trip-photo-input]',c),clear=$('[data-clear-photos]',c);input.onchange=function(){var files=Array.prototype.slice.call(input.files||[]).filter(function(f){return/^image\//.test(f.type)}).slice(0,6);Promise.all(files.map(function(f,i){return fileData(f).then(function(dataUrl){return put({id:'trip-photo-'+Date.now()+'-'+i+'-'+Math.random().toString(36).slice(2,7),day:day,name:f.name,dataUrl:dataUrl,createdAt:Date.now()+i})})})).then(function(){input.value='';return renderPhotos(c,day)}).then(function(){toast(window.CloudTicketVault?'样片已上传并共享':'样片已保存到本机')}).catch(function(){toast('样片保存失败')})};clear.onclick=function(){records(day).then(function(items){return Promise.all(items.map(function(x){return del(x.id)}))}).then(function(){renderPhotos(c,day)}).catch(function(){toast('清空未完成，请重试。')})}}
 function decisions(d){return '<div class="audit-day-decisions">'+[['可取消项','cancellable'],['替代方案','fallback'],['今晚确认','tonight']].map(function(x){return '<p><b>'+x[0]+'</b><span>'+esc(d[x[1]]||'')+'</span></p>'}).join('')+'</div>'}
 function outfits(n){var a=n.outfit_advice||{};return [['women','女生穿搭'],['men','男生穿搭'],['practical_note','当天调整']].map(function(pair){return typeof a[pair[0]]==='string'&&a[pair[0]].trim()?'<article class="trip-shot trip-outfit"><b>'+pair[1]+'</b><div><span>'+esc(a[pair[0]])+'</span></div></article>':''}).join('')}
 function photo(d,di){var n=d.photo_advice||{},plans=n.shooting_plan||[];if(!plans.length)plans=(n.suitable_shots||[]).slice(0,3).map(function(note,i){return{time:i===0?'光线':'机位 '+(i+1),title:n.title||'当天拍摄',note:note}});var cards=plans.map(function(p){return'<article class="trip-shot"><b>'+esc(p.time||'建议')+'</b><div><strong>'+esc(p.title||n.title)+'</strong><span>'+esc(p.note)+'</span></div></article>'}).join('');if(n.portrait_tip)cards+='<article class="trip-shot"><b>人像</b><div><strong>摄影与穿搭提醒</strong><span>'+esc(n.portrait_tip)+'</span></div></article>';cards+=outfits(n);return'<section class="trip-photo-card"><header><div><small>PHOTO NOTES · 摄影与穿搭</small><h3>'+esc(n.title||'摄影与穿搭')+'</h3></div><span class="trip-photo-count">0 张样片</span></header>'+(n.lighting?'<p class="trip-lighting">'+esc(n.lighting)+'</p>':'')+''+decisions(d)+'<div class="trip-shot-list">'+cards+'</div><div class="trip-photo-tools"><label>＋ 上传参考样片<input type="file" accept="image/*" multiple data-trip-photo-input></label><button type="button" data-clear-photos disabled>清空样片</button></div><small class="trip-photo-note">'+(window.CloudTicketVault?'样片上传后会在这本手册中共享。':'样片保存在本机，刷新后仍保留。')+'</small><div class="trip-uploaded-grid"></div></section>'}
-function copyDay(d){var lines=[d.date+' · '+d.theme,d.summary||''];(d.stops||[]).forEach(function(s,i){lines.push((i+1)+'. '+(s.time||'时间待定')+' '+s.name+(s.practical_note?'｜'+s.practical_note:''))});if(navigator.clipboard&&window.isSecureContext)return navigator.clipboard.writeText(lines.filter(Boolean).join('\n'));var area=document.createElement('textarea');area.value=lines.filter(Boolean).join('\n');document.body.appendChild(area);area.select();if(!document.execCommand("copy")){area.remove();return Promise.reject(new Error("复制失败"))};area.remove();return Promise.resolve()}
+function copyDay(d){var lines=[d.date+' · '+d.theme,d.summary||''];(d.stops||[]).forEach(function(s,i){lines.push((i+1)+'. '+(s.time||'时间待定')+' '+s.name+(s.practical_note?'｜'+s.practical_note:''))});return window.TravelMobileRuntime.copyText(lines.filter(Boolean).join('\n'))}
 function selectView(panel,view){
  panel.dataset.tripView=view;
  var shell=$('.trip-mode-panel',panel);if(shell)shell.dataset.tripView=view;
@@ -53,17 +67,17 @@
 function hideViewer(){var v=$('.trip-photo-viewer');if(v){v.classList.remove('is-open');$('img',v).removeAttribute('src')}}function toast(m){var t=$('.trip-mode-toast');if(!t){t=document.createElement('div');t.className='trip-mode-toast';document.body.appendChild(t)}t.textContent=m;t.classList.add('is-visible');clearTimeout(t._timer);t._timer=setTimeout(function(){t.classList.remove('is-visible')},1800)}
 function openXhs(a){var q=(a&&a.getAttribute("data-xhs-keyword"))||"@@DISPLAY_NAME@@旅行";var api=window.xhs&&window.xhs.miniTool;if(api&&api.openRedPage){api.openRedPage({type:"search",params:{keyword:q}});return}if(window.miniToolToast)window.miniToolToast("请在小红书中搜索："+q)}
 var TICKET_DB='travel-ticket-vault-v1',TICKET_STORE='items';
-function ticketDb(){return new Promise(function(resolve,reject){var r=indexedDB.open(TICKET_DB,1);r.onupgradeneeded=function(){if(!r.result.objectStoreNames.contains(TICKET_STORE))r.result.createObjectStore(TICKET_STORE,{keyPath:'id'})};r.onsuccess=function(){resolve(r.result)};r.onerror=function(){reject(r.error)}})}
-function ticketAll(key){if(window.CloudTicketVault)return window.CloudTicketVault.all(key);return ticketDb().then(function(db){return new Promise(function(resolve,reject){var r=db.transaction(TICKET_STORE).objectStore(TICKET_STORE).getAll();r.onsuccess=function(){resolve(r.result.filter(function(x){return x.placeKey===key}).sort(function(a,b){return b.createdAt-a.createdAt}))};r.onerror=function(){reject(r.error)}})})}
-function ticketPut(x){if(window.CloudTicketVault)return window.CloudTicketVault.put(x);return ticketDb().then(function(db){return new Promise(function(resolve,reject){var r=db.transaction(TICKET_STORE,'readwrite').objectStore(TICKET_STORE).put(x);r.onsuccess=resolve;r.onerror=function(){reject(r.error)}})})}
-function ticketDelete(id){if(window.CloudTicketVault)return window.CloudTicketVault.delete(id);return ticketDb().then(function(db){return new Promise(function(resolve,reject){var r=db.transaction(TICKET_STORE,'readwrite').objectStore(TICKET_STORE).delete(id);r.onsuccess=resolve;r.onerror=function(){reject(r.error)}})})}
+function ticketDb(){return window.TravelMobileRuntime.openStore(TICKET_DB,TICKET_STORE)}
+function ticketAll(key){if(window.CloudTicketVault)return window.CloudTicketVault.all(key);return window.TravelMobileRuntime.operation(TICKET_DB,TICKET_STORE,'getAll').then(function(items){return items.filter(function(x){return x.placeKey===key}).sort(function(a,b){return b.createdAt-a.createdAt})})}
+function ticketPut(x){if(window.CloudTicketVault)return window.CloudTicketVault.put(x);return window.TravelMobileRuntime.operation(TICKET_DB,TICKET_STORE,'put',x)}
+function ticketDelete(id){if(window.CloudTicketVault)return window.CloudTicketVault.delete(id);return window.TravelMobileRuntime.operation(TICKET_DB,TICKET_STORE,'delete',id)}
 function ticketLabel(x){var n=x.title||x.fileName||'附件';return n.length>10?n.slice(0,9)+'…':n}
-function showTicketContent(x){if(!x.url&&/Android|iPhone|iPad|iPod/i.test(navigator.userAgent)&&(/\.pdf$/i.test(x.fileName||'')||x.contentType==='application/pdf'||/^data:application\/pdf/i.test(x.dataUrl||''))){window.open(x.dataUrl,'_blank','noopener');return}var modal=document.createElement('div');modal.className='trip-ticket-preview'+(x.url?' is-link':'');var pdf=/\.pdf$/i.test(x.fileName||'')||/^data:application\/pdf/i.test(x.dataUrl||'');var body=x.url?'<div class="trip-ticket-link-preview"><a href="'+esc(x.url)+'" target="_blank" rel="noreferrer">打开链接</a><small>'+esc(x.url)+'</small></div>':(pdf?'<iframe title="PDF 预览" src="'+esc(x.dataUrl)+'"></iframe>':'<img src="'+esc(x.dataUrl)+'" alt="'+esc(x.title||x.fileName||'票据图片')+'">');modal.innerHTML='<section role="dialog" aria-modal="true" aria-label="收藏预览"><header><b>'+esc(x.title||x.fileName||'收藏')+'</b><span><button type="button" data-preview-delete>删除</button><button type="button" data-preview-close aria-label="关闭预览">×</button></span></header>'+body+'</section>';document.body.appendChild(modal);function close(){modal.remove()}$('[data-preview-close]',modal).onclick=close;$('[data-preview-delete]',modal).onclick=function(){if(!confirm('删除这个收藏按钮吗？'))return;ticketDelete(x.id).then(function(){close();renderTicketShortcuts(document);toast('收藏已删除')})};modal.onclick=function(e){if(e.target===modal)close()}}
-function renderTicketShortcuts(root){$$('[data-ticket-shortcuts]',root||document).forEach(function(box){ticketAll(box.dataset.ticketKey).then(function(items){box.innerHTML='';items.forEach(function(x){var b=document.createElement('button');b.type='button';b.className='trip-ticket-shortcut';b.textContent=ticketLabel(x);b.title=x.title||x.fileName||'打开附件';b.onclick=function(){showTicketContent(x)};box.appendChild(b)})})})}
-function renderTicketList(box,key){return ticketAll(key).then(function(items){box.innerHTML=items.length?items.map(function(x){return'<article><button type="button" class="trip-ticket-open" data-ticket-open="'+esc(x.id)+'"><b>'+esc(x.title||x.fileName||'收藏项目')+'</b><small>'+esc(x.url?'打开链接':'直接预览')+'</small></button><span class="trip-ticket-row-actions"><button type="button" data-ticket-edit="'+esc(x.id)+'">编辑</button><button type="button" data-ticket-delete="'+esc(x.id)+'">删除</button></span></article>'}).join(''):'<p>还没有收藏。可保存票价页、小红书链接、PDF或票据图片。</p>';$$('[data-ticket-open]',box).forEach(function(b){var x=items.find(function(item){return item.id===b.dataset.ticketOpen});b.onclick=function(){if(x)showTicketContent(x)}});$$('[data-ticket-edit]',box).forEach(function(b){var x=items.find(function(item){return item.id===b.dataset.ticketEdit});b.onclick=function(){if(!x)return;var title=prompt('修改按钮名称',x.title||x.fileName||'附件');if(title===null)return;title=title.trim();if(!title)return;ticketPut(Object.assign({},x,{title:title})).then(function(){renderTicketList(box,key);renderTicketShortcuts(document);toast('名称已更新')})}});$$('[data-ticket-delete]',box).forEach(function(b){b.onclick=function(){if(!confirm('删除这个收藏吗？'))return;ticketDelete(b.dataset.ticketDelete).then(function(){renderTicketList(box,key);renderTicketShortcuts(document);toast('收藏已删除')})}})})}
+function showTicketContent(x){var pdf=!x.url&&(/\.pdf$/i.test(x.fileName||'')||x.contentType==='application/pdf'||/^data:application\/pdf/i.test(x.dataUrl||''));if(pdf){try{var ui=window.TravelMobileRuntime.sheet(x.title||x.fileName||'PDF收藏'),blob=window.TravelMobileRuntime.fromData(x.dataUrl),file=window.TravelMobileRuntime.fileControls(ui.panel,blob,x.fileName||'收藏.pdf'),frame=document.createElement('iframe');frame.title=x.title||'PDF预览';frame.src=file.url;frame.style.cssText='display:block;width:100%;height:55vh;border:0';ui.panel.append(frame);ui.box.cleanup=file.cleanup;return}catch(e){toast('PDF读取失败，原收藏仍保留。');return}}var modal=document.createElement('div');modal.className='trip-ticket-preview'+(x.url?' is-link':'');var body=x.url?'<div class="trip-ticket-link-preview"><a href="'+esc(x.url)+'" target="_blank" rel="noreferrer">打开链接</a><small>'+esc(x.url)+'</small></div>':'<img src="'+esc(x.dataUrl)+'" alt="'+esc(x.title||x.fileName||'票据图片')+'">';modal.innerHTML='<section role="dialog" aria-modal="true" aria-label="收藏预览"><header><b>'+esc(x.title||x.fileName||'收藏')+'</b><span><button type="button" data-preview-delete>删除</button><button type="button" data-preview-close aria-label="关闭预览">×</button></span></header>'+body+'</section>';document.body.appendChild(modal);function close(){modal.remove()}$('[data-preview-close]',modal).onclick=close;$('[data-preview-delete]',modal).onclick=function(){if(!confirm('删除这个收藏按钮吗？'))return;ticketDelete(x.id).then(function(){close();renderTicketShortcuts(document);toast('收藏已删除')}).catch(function(){toast('删除未完成，收藏仍保留。')})};modal.onclick=function(e){if(e.target===modal)close()}}
+function renderTicketShortcuts(root){$$('[data-ticket-shortcuts]',root||document).forEach(function(box){ticketAll(box.dataset.ticketKey).then(function(items){box.innerHTML='';items.forEach(function(x){var b=document.createElement('button');b.type='button';b.className='trip-ticket-shortcut';b.textContent=ticketLabel(x);b.title=x.title||x.fileName||'打开附件';b.onclick=function(){showTicketContent(x)};box.appendChild(b)})}).catch(function(){box.textContent='本机收藏暂时无法读取；可点收藏入口重试。'})})}
+function renderTicketList(box,key){return ticketAll(key).then(function(items){box.innerHTML=items.length?items.map(function(x){return'<article><button type="button" class="trip-ticket-open" data-ticket-open="'+esc(x.id)+'"><b>'+esc(x.title||x.fileName||'收藏项目')+'</b><small>'+esc(x.url?'打开链接':'直接预览')+'</small></button><span class="trip-ticket-row-actions"><button type="button" data-ticket-edit="'+esc(x.id)+'">编辑</button><button type="button" data-ticket-delete="'+esc(x.id)+'">删除</button></span></article>'}).join(''):'<p>还没有收藏。可保存票价页、小红书链接、PDF或票据图片。</p>';$$('[data-ticket-open]',box).forEach(function(b){var x=items.find(function(item){return item.id===b.dataset.ticketOpen});b.onclick=function(){if(x)showTicketContent(x)}});$$('[data-ticket-edit]',box).forEach(function(b){var x=items.find(function(item){return item.id===b.dataset.ticketEdit});b.onclick=function(){if(!x)return;var title=prompt('修改按钮名称',x.title||x.fileName||'附件');if(title===null)return;title=title.trim();if(!title)return;ticketPut(Object.assign({},x,{title:title})).then(function(){renderTicketList(box,key);renderTicketShortcuts(document);toast('名称已更新')}).catch(function(){toast('改名未保存，请重试。')})}});$$('[data-ticket-delete]',box).forEach(function(b){b.onclick=function(){if(!confirm('删除这个收藏吗？'))return;ticketDelete(b.dataset.ticketDelete).then(function(){renderTicketList(box,key);renderTicketShortcuts(document);toast('收藏已删除')}).catch(function(){toast('删除未完成，收藏仍保留。')})}})}).catch(function(){box.textContent='收藏暂时无法读取，请检查浏览器存储设置后重试。'})}
 function extractSavedUrl(value){var text=String(value||'').trim(),match=text.match(/https?:\/\/[^\s\]）)>}]+/i);var candidate=(match&&match[0]||text).replace(/[，。；、（(【]+$/,'');if(!/^https?:\/\//i.test(candidate)&&/^(?:www\.)?(?:xhslink\.(?:cn|com)|xiaohongshu\.com)\//i.test(candidate))candidate='https://'+candidate.replace(/^www\./i,'www.');try{var parsed=new URL(candidate);return /^https?:$/.test(parsed.protocol)?parsed.href:''}catch(e){return''}}
-function openTicketVault(button){var key=button.dataset.ticketKey,name=button.dataset.ticketName,modal=document.createElement('div');modal.className='trip-ticket-overlay';modal.innerHTML='<section class="trip-ticket-panel" role="dialog" aria-modal="true" aria-label="票价收藏夹"><header><div><small>TICKET VAULT · 本机保存</small><h3>'+esc(name)+'</h3></div><button type="button" data-ticket-close aria-label="关闭">×</button></header><form><label>按钮名称（可选）<input name="title" maxlength="60" placeholder="例如：门票 / 预约单 / 小红书攻略"></label><label>网页或小红书分享链接<input name="url" type="text" inputmode="url" placeholder="可粘贴网址或整段小红书分享文字"></label><button type="submit">保存链接</button><label class="trip-ticket-file">＋ 上传 PDF 或图片<input name="file" type="file" accept="application/pdf,image/*"></label></form><div class="trip-ticket-list"></div><p class="trip-ticket-note">支持普通网页、小红书分享文字、图片和 PDF；保存后均可编辑或删除。内容只保存在当前设备。</p></section>';document.body.appendChild(modal);var list=$('.trip-ticket-list',modal),form=$('form',modal),file=form.elements.file;function close(){modal.remove()}$('[data-ticket-close]',modal).onclick=close;modal.onclick=function(e){if(e.target===modal)close()};form.onsubmit=function(e){e.preventDefault();var url=extractSavedUrl(form.elements.url.value);if(!url){toast('没有识别到有效链接');return}var isXhs=/xhslink\.(?:cn|com)|xiaohongshu\.com/i.test(url);ticketPut({id:crypto.randomUUID(),placeKey:key,title:form.elements.title.value.trim()||(isXhs?'小红书攻略':url),url:url,createdAt:Date.now()}).then(function(){form.reset();renderTicketShortcuts(document);toast('链接已收藏');close()})};file.onchange=function(){var f=file.files[0];if(!f)return;if(f.size>20*1024*1024){toast('文件请控制在20MB以内');file.value='';return}var suggested=form.elements.title.value.trim()||f.name.replace(/\.[^.]+$/,'');var title=prompt('给这个附件按钮命名',suggested);if(title===null){file.value='';return}title=title.trim()||suggested;fileData(f).then(function(dataUrl){return ticketPut({id:crypto.randomUUID(),placeKey:key,title:title,fileName:f.name,dataUrl:dataUrl,createdAt:Date.now()})}).then(function(){file.value='';form.elements.title.value='';renderTicketList(list,key);renderTicketShortcuts(document);toast('附件按钮已生成')}).catch(function(){toast('文件保存失败')})};renderTicketList(list,key)}
-function openTicketVaultV2(button){var key=button.dataset.ticketKey,name=button.dataset.ticketName,modal=document.createElement('div'),selectedFile=null;modal.className='trip-ticket-overlay';modal.innerHTML='<section class="trip-ticket-panel" role="dialog" aria-modal="true" aria-label="收藏夹"><header><div><small>TICKET VAULT · 本机保存</small><h3>'+esc(name)+'</h3></div><button type="button" data-ticket-close aria-label="关闭">×</button></header><form><label>按钮名称<input name="title" maxlength="60" placeholder="例如：门票 / 预约单 / 小红书攻略"></label><label>网页或小红书分享链接（可选）<input name="url" type="text" inputmode="url" placeholder="可粘贴网址或整段小红书分享文字"></label><label class="trip-ticket-file">＋ 选择 PDF 或图片<input name="file" type="file" accept="application/pdf,image/*"></label><span class="trip-ticket-file-name" data-ticket-file-name>尚未选择文件</span><button class="trip-ticket-save" type="submit">保存收藏</button></form><div class="trip-ticket-list"></div><p class="trip-ticket-note">填写的按钮名称同时用于链接、图片和 PDF。粘贴链接或选择一个文件后，点击“保存收藏”生成按钮。</p></section>';document.body.appendChild(modal);var list=$('.trip-ticket-list',modal),form=$('form',modal),file=form.elements.file,fileName=$('[data-ticket-file-name]',modal);function close(){modal.remove()}$('[data-ticket-close]',modal).onclick=close;modal.onclick=function(e){if(e.target===modal)close()};file.onchange=function(){selectedFile=file.files[0]||null;fileName.textContent=selectedFile?selectedFile.name:'尚未选择文件'};form.onsubmit=function(e){e.preventDefault();var raw=form.elements.url.value.trim(),url=raw?extractSavedUrl(raw):'',title=form.elements.title.value.trim();if(raw&&!url){toast('没有识别到有效链接');return}if(selectedFile){if(selectedFile.size>20*1024*1024){toast('文件请控制在20MB以内');return}var f=selectedFile,fileTitle=title||f.name.replace(/\.[^.]+$/,'');fileData(f).then(function(dataUrl){return ticketPut({id:crypto.randomUUID(),placeKey:key,title:fileTitle,fileName:f.name,dataUrl:dataUrl,createdAt:Date.now()})}).then(function(){renderTicketShortcuts(document);toast((/^image\//.test(f.type)?'图片':'PDF')+'已收藏');close()}).catch(function(){toast('文件保存失败')});return}if(url){var isXhs=/xhslink\.(?:cn|com)|xiaohongshu\.com/i.test(url);ticketPut({id:crypto.randomUUID(),placeKey:key,title:title||(isXhs?'小红书攻略':url),url:url,createdAt:Date.now()}).then(function(){renderTicketShortcuts(document);toast('链接已收藏');close()});return}toast('请粘贴链接或选择图片/PDF')};renderTicketList(list,key)}
+function openTicketVault(button){var key=button.dataset.ticketKey,name=button.dataset.ticketName,modal=document.createElement('div');modal.className='trip-ticket-overlay';modal.innerHTML='<section class="trip-ticket-panel" role="dialog" aria-modal="true" aria-label="票价收藏夹"><header><div><small>TICKET VAULT · 本机保存</small><h3>'+esc(name)+'</h3></div><button type="button" data-ticket-close aria-label="关闭">×</button></header><form><label>按钮名称（可选）<input name="title" maxlength="60" placeholder="例如：门票 / 预约单 / 小红书攻略"></label><label>网页或小红书分享链接<input name="url" type="text" inputmode="url" placeholder="可粘贴网址或整段小红书分享文字"></label><button type="submit">保存链接</button><label class="trip-ticket-file">＋ 上传 PDF 或图片<input name="file" type="file" accept="application/pdf,image/*"></label></form><div class="trip-ticket-list"></div><p class="trip-ticket-note">支持普通网页、小红书分享文字、图片和 PDF；保存后均可编辑或删除。内容只保存在当前设备。</p></section>';document.body.appendChild(modal);var list=$('.trip-ticket-list',modal),form=$('form',modal),file=form.elements.file;function close(){modal.remove()}$('[data-ticket-close]',modal).onclick=close;modal.onclick=function(e){if(e.target===modal)close()};form.onsubmit=function(e){e.preventDefault();var url=extractSavedUrl(form.elements.url.value);if(!url){toast('没有识别到有效链接');return}var isXhs=/xhslink\.(?:cn|com)|xiaohongshu\.com/i.test(url);ticketPut({id:window.TravelMobileRuntime.uid(),placeKey:key,title:form.elements.title.value.trim()||(isXhs?'小红书攻略':url),url:url,createdAt:Date.now()}).then(function(){form.reset();renderTicketShortcuts(document);toast('链接已收藏');close()}).catch(function(){toast('链接未保存，输入已保留；请检查本机存储。')})};file.onchange=function(){var f=file.files[0];if(!f)return;if(f.size>20*1024*1024){toast('文件请控制在20MB以内');file.value='';return}var suggested=form.elements.title.value.trim()||f.name.replace(/\.[^.]+$/,'');var title=prompt('给这个附件按钮命名',suggested);if(title===null){file.value='';return}title=title.trim()||suggested;fileData(f).then(function(dataUrl){return ticketPut({id:window.TravelMobileRuntime.uid(),placeKey:key,title:title,fileName:f.name,dataUrl:dataUrl,createdAt:Date.now()})}).then(function(){file.value='';form.elements.title.value='';renderTicketList(list,key);renderTicketShortcuts(document);toast('附件按钮已生成')}).catch(function(){toast('文件保存失败')})};renderTicketList(list,key)}
+function openTicketVaultV2(button){var key=button.dataset.ticketKey,name=button.dataset.ticketName,modal=document.createElement('div'),selectedFile=null;modal.className='trip-ticket-overlay';modal.innerHTML='<section class="trip-ticket-panel" role="dialog" aria-modal="true" aria-label="收藏夹"><header><div><small>TICKET VAULT · 本机保存</small><h3>'+esc(name)+'</h3></div><button type="button" data-ticket-close aria-label="关闭">×</button></header><form><label>按钮名称<input name="title" maxlength="60" placeholder="例如：门票 / 预约单 / 小红书攻略"></label><label>网页或小红书分享链接（可选）<input name="url" type="text" inputmode="url" placeholder="可粘贴网址或整段小红书分享文字"></label><label class="trip-ticket-file">＋ 选择 PDF 或图片<input name="file" type="file" accept="application/pdf,image/*"></label><span class="trip-ticket-file-name" data-ticket-file-name>尚未选择文件</span><button class="trip-ticket-save" type="submit">保存收藏</button></form><div class="trip-ticket-list"></div><p class="trip-ticket-note">填写的按钮名称同时用于链接、图片和 PDF。粘贴链接或选择一个文件后，点击“保存收藏”生成按钮。</p></section>';document.body.appendChild(modal);var list=$('.trip-ticket-list',modal),form=$('form',modal),file=form.elements.file,fileName=$('[data-ticket-file-name]',modal);function close(){modal.remove()}$('[data-ticket-close]',modal).onclick=close;modal.onclick=function(e){if(e.target===modal)close()};file.onchange=function(){selectedFile=file.files[0]||null;fileName.textContent=selectedFile?selectedFile.name:'尚未选择文件'};form.onsubmit=function(e){e.preventDefault();var raw=form.elements.url.value.trim(),url=raw?extractSavedUrl(raw):'',title=form.elements.title.value.trim();if(raw&&!url){toast('没有识别到有效链接');return}if(selectedFile){if(selectedFile.size>20*1024*1024){toast('文件请控制在20MB以内');return}var f=selectedFile,fileTitle=title||f.name.replace(/\.[^.]+$/,'');fileData(f).then(function(dataUrl){return ticketPut({id:window.TravelMobileRuntime.uid(),placeKey:key,title:fileTitle,fileName:f.name,dataUrl:dataUrl,createdAt:Date.now()})}).then(function(){renderTicketShortcuts(document);toast((/^image\//.test(f.type)?'图片':'PDF')+'已收藏');close()}).catch(function(){toast('文件保存失败')});return}if(url){var isXhs=/xhslink\.(?:cn|com)|xiaohongshu\.com/i.test(url);ticketPut({id:window.TravelMobileRuntime.uid(),placeKey:key,title:title||(isXhs?'小红书攻略':url),url:url,createdAt:Date.now()}).then(function(){renderTicketShortcuts(document);toast('链接已收藏');close()}).catch(function(){toast('链接未保存，输入已保留；请检查本机存储。')});return}toast('请粘贴链接或选择图片/PDF')};renderTicketList(list,key)}
 function init(){var b=document.createElement('button');b.className='trip-mode-launch';b.textContent='旅行';b.setAttribute('aria-label','旅行模式');b.onclick=open;document.body.appendChild(b);document.addEventListener('click',function(e){var ticket=e.target.closest&&e.target.closest('[data-ticket-add]');if(ticket){openTicketVaultV2(ticket);return}var a=e.target.closest&&e.target.closest('.is-xhs');if(a&&window.xhs&&window.xhs.miniTool){e.preventDefault();openXhs(a)}});document.addEventListener('keydown',function(e){if(e.key==='Escape'){hideViewer();var p=$('.trip-mode-overlay');if(p)close(p)}})}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
 })();
--- a/itinerary-customizer.js
+++ b/itinerary-customizer.js
@@ -2,10 +2,17 @@
 function $(s,c){return(c||document).querySelector(s)}function $$(s,c){return Array.from((c||document).querySelectorAll(s))}function text(s,c){var n=$(s,c);return n?n.textContent.trim().replace(/\s+/g,' '):''}function esc(s){return String(s||'').replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]})}function uid(){return Date.now().toString(36)+Math.random().toString(36).slice(2,7)}function copy(x){return JSON.parse(JSON.stringify(x))}
 function sourceDays(){return $$('#route .day').map(function(day,i){return{day:i+1,date:text('summary small',day),title:text('summary b',day)||('第 '+(i+1)+' 天'),places:$$('.route-stop',day).map(function(stop,j){var seg=stop.closest('.route-map-segment');return{id:'source-'+i+'-'+j,order:j+1,name:text('strong',stop),arrival:text('time',stop),dwell:text('.route-dwell',stop).replace(/^逗留[：:]\s*/,''),transport:text('.route-leg span',seg),travelTime:text('.route-leg b',seg),distance:text('.route-leg small',seg),map:stop.href||'',isNew:false}})}})}
 function clock(raw){var m=String(raw||'').match(/(\d{1,2})[:：](\d{2})/);return m?Number(m[1])*60+Number(m[2]):720}function timeValue(raw){var m=String(raw||'').match(/(\d{1,2})[:：](\d{2})/);return m?String(m[1]).padStart(2,'0')+':'+m[2]:''}function period(p){var t=clock(p.arrival);return t<720?'morning':t<1080?'afternoon':'evening'}function theme(day){return day.title||('第 '+day.day+' 天')}function shortDate(d){var m=d.date.match(/\d{2}\.\d{2}/);return m?m[0]:'第 '+d.day+' 天'}
-function payload(ops,original,note,photos){return{referenceImages:Object.keys(photos||{}).reduce(function(all,key){return all.concat((photos[key]||[]).map(function(x){return{day:Number(key)+1,name:x.name,src:x.src}}))},[]),schema:'travel-guide-guided-request-v6',guide:'@@YEAR@@ @@DISPLAY_NAME@@旅行手册',createdAt:new Date().toISOString(),operations:ops,freeformSuggestion:note||'',currentItinerary:original,agentInstruction:'逐项执行 operations 与 freeformSuggestion。同步更新每日行文、Mini Route、前后交通时间及相关介绍模块。新增景点、餐厅、购物、瑜伽健身舞蹈或 SPA 时须进入相应介绍。空白字段代表允许 Agent 结合路线主动补齐。交通、停留建议、评分、开放时间、图片与描述须使用可靠来源。'}}function prompt(ops,original,note){var p=payload(ops,original,note);return'请按以下申请更新旅行手册：\n\n'+ops.map(function(o,i){return(i+1)+'. '+o.sentence}).join('\n')+(note?'\n\n补充建议：\n'+note:'')+'\n\n当前发布版行程：\n'+JSON.stringify(original,null,2)+'\n\n执行要求：\n'+p.agentInstruction}function download(p){var a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(p,null,2)],{type:'application/json'}));a.download='@@DISPLAY_NAME@@攻略修改申请-'+new Date().toISOString().slice(0,10)+'.json';a.click();setTimeout(function(){URL.revokeObjectURL(a.href)},500)}
-function copyText(value){if(navigator.clipboard&&window.isSecureContext)return navigator.clipboard.writeText(value);var area=document.createElement('textarea');area.value=value;document.body.appendChild(area);area.select();var ok=document.execCommand('copy');area.remove();return ok?Promise.resolve():Promise.reject(new Error('复制失败，请使用导出'))}
+function payload(ops,original,note,photos){return{referenceImages:Object.keys(photos||{}).reduce(function(all,key){return all.concat((photos[key]||[]).map(function(x){return{day:Number(key)+1,name:x.name,src:x.src}}))},[]),schema:'travel-guide-guided-request-v6',guide:'@@YEAR@@ @@DISPLAY_NAME@@旅行手册',createdAt:new Date().toISOString(),operations:ops,freeformSuggestion:note||'',currentItinerary:original,agentInstruction:'逐项执行 operations 与 freeformSuggestion。同步更新每日行文、Mini Route、前后交通时间及相关介绍模块。新增景点、餐厅、购物、瑜伽健身舞蹈或 SPA 时须进入相应介绍。空白字段代表允许 Agent 结合路线主动补齐。交通、停留建议、评分、开放时间、图片与描述须使用可靠来源。'}}function prompt(ops,original,note){var p=payload(ops,original,note);return'请按以下申请更新旅行手册：\n\n'+ops.map(function(o,i){return(i+1)+'. '+o.sentence}).join('\n')+(note?'\n\n补充建议：\n'+note:'')+'\n\n当前发布版行程：\n'+JSON.stringify(original,null,2)+'\n\n执行要求：\n'+p.agentInstruction}function download(p){return window.TravelMobileRuntime.offerFile(new Blob([JSON.stringify(p,null,2)],{type:'application/json'}),'@@DISPLAY_NAME@@攻略修改申请-'+new Date().toISOString().slice(0,10)+'.json')}
+function copyText(value){return window.TravelMobileRuntime.copyText(value)}
 function init(){if($('#itinerary-customizer'))return;var original=sourceDays(),draft=copy(original),ops=[],selectedDay=0,photoRefs={};var launch=document.createElement('button');launch.className='itinerary-customizer-launch';launch.textContent='调整';launch.setAttribute('aria-label','调整行程');document.body.appendChild(launch);var modal=document.createElement('div');modal.className='itinerary-customizer';modal.id='itinerary-customizer';modal.hidden=true;modal.innerHTML='<section class="itinerary-customizer__panel" role="dialog" aria-modal="true"><header class="itinerary-customizer__head"><div><small>ITINERARY DRAFT</small><h2>直接调整每日行程</h2><p>点时间、地点或备注即可编辑；按住左侧手柄拖动排序，也可点行程间的小加号新增。</p></div><button class="itinerary-customizer__close" aria-label="关闭">×</button></header><div class="itinerary-customizer__body"><section class="custom-step"><header><b>01</b><span>选择日期</span></header><div class="custom-days"></div></section><section class="custom-step custom-existing"><header><b>02</b><span>当日行程</span><button class="custom-restore" data-restore>还原当日</button></header><div class="custom-timeline"></div></section><section class="custom-step custom-form"><header><b>03</b><span>快速增加地点</span><small>按时间自动排列</small></header><div class="custom-add-form"><label><span>类型</span><select data-kind><option>景点</option><option>餐厅</option><option>购物或店铺</option><option>瑜伽或健身</option><option>舞蹈体验</option><option>SPA</option><option>其他地点</option></select></label><label><span>名称（可自由描述，也可留空）</span><input data-name placeholder="如：海边咖啡馆；留空则由 Agent 推荐"></label><label><span>时间（可留空）</span><input data-time type="time"></label><button class="custom-confirm" data-add>＋ 加入当天</button></div></section><section class="custom-step custom-photo-ref"><header><b>04</b><span>上传参考样片</span><small>同步显示在旅行模式 · 参考图片保存在本机；需要发送时请单独附图</small></header><label class="custom-photo-add">＋ 选择人像或构图参考<input type="file" accept="image/*" multiple data-photo-input></label><div class="custom-photo-preview"></div></section><section class="custom-step custom-freeform"><header><b>05</b><span>补充建议</span><small>选填</small></header><textarea data-freeform placeholder="例如：这一天不要太赶，希望午餐附近有咖啡店；其余细节请帮我判断。"></textarea></section><section class="custom-request"><header><div><small>AUTO-GENERATED</small><b>修改申请</b></div><em></em></header><div class="custom-request-list"></div></section><div class="itinerary-customizer__actions"><button class="is-primary" data-copy>复制文字申请</button><button data-download>下载完整申请包</button></div><p class="itinerary-customizer__status" aria-live="polite"></p></div></section>';document.body.appendChild(modal);var status=$('.itinerary-customizer__status',modal);
-function renderDays(){var w=$('.custom-days',modal);w.innerHTML=draft.map(function(d,i){return'<button class="'+(i===selectedDay?'is-active':'')+'" data-day="'+i+'" aria-label="第 '+d.day+' 天，'+esc(theme(d))+'"><b>'+String(d.day).padStart(2,'0')+'</b><span><strong>'+esc(shortDate(d))+'</strong><small>'+esc(theme(d))+'</small></span></button>'}).join('');$$('[data-day]',w).forEach(function(b){b.onclick=function(){selectedDay=+b.dataset.day;renderAll()}})}
+var storageReady=false,saveQueue=Promise.resolve(),draftKey='draft:'+((window.HANDBOOK_CONFIG||{}).destination||document.title),originalSignature=JSON.stringify(original),saveStatus=document.createElement('p');saveStatus.className='customizer-save-status';saveStatus.setAttribute('role','status');status.before(saveStatus);saveStatus.textContent='正在读取本机草稿…';
+function draftSnapshot(){return{id:draftKey,schema:1,original:copy(original),originalSignature:originalSignature,draft:copy(draft),ops:copy(ops),photoRefs:copy(photoRefs),freeform:$('[data-freeform]',modal).value,selectedDay:selectedDay,savedAt:new Date().toISOString()}}
+function persistDraft(){if(!storageReady)return;var snapshot=draftSnapshot();saveStatus.textContent='正在保存草稿…';saveQueue=saveQueue.catch(function(){}).then(function(){return window.TravelMobileRuntime.operation('travel-guide-customizer-v1','drafts','put',snapshot)}).then(function(){saveStatus.textContent='草稿与参考图片已保存在本机。'},function(){saveStatus.textContent='保存失败；编辑仍在当前页面，请立即下载完整申请包，勿刷新。'});return saveQueue}
+function validDraft(value){return value&&value.schema===1&&value.originalSignature===originalSignature&&JSON.stringify(value.original)===originalSignature&&Array.isArray(value.draft)&&value.draft.length===original.length&&value.draft.every(function(d,i){return d.day===original[i].day&&typeof d.title==='string'&&Array.isArray(d.places)&&d.places.every(function(p){return p&&typeof p.id==='string'&&typeof p.name==='string'})})&&Array.isArray(value.ops)&&value.ops.every(function(o){return o&&typeof o.sentence==='string'&&Number.isInteger(o.day)&&o.day>=1&&o.day<=original.length})&&typeof value.freeform==='string'&&value.photoRefs&&typeof value.photoRefs==='object'&&!Array.isArray(value.photoRefs)&&Object.keys(value.photoRefs).every(function(k){return /^\d+$/.test(k)&&Number(k)<original.length&&Array.isArray(value.photoRefs[k])&&value.photoRefs[k].every(function(x){return x&&typeof x.id==='string'&&typeof x.src==='string'&&/^data:image\//.test(x.src)})})}
+var restored=window.TravelMobileRuntime.operation('travel-guide-customizer-v1','drafts','get',draftKey).then(function(saved){if(saved){if(validDraft(saved)){draft=saved.draft;ops=saved.ops;photoRefs=saved.photoRefs;selectedDay=Math.max(0,Math.min(original.length-1,Number(saved.selectedDay)||0));$('[data-freeform]',modal).value=saved.freeform;Object.keys(photoRefs).forEach(function(k){photoRefs[k].forEach(function(ref){appendPhotoRef(Number(k),ref)})});saveStatus.textContent='已恢复本机草稿与参考图片。'}else saveStatus.textContent='旧草稿格式损坏或原行程已更新，已使用原稿；旧存储未删除。'}else saveStatus.textContent='尚无本机草稿，修改后将保存。';storageReady=true;renderAll()},function(){storageReady=true;saveStatus.textContent='本机草稿暂时无法读取；可继续编辑，并下载完整申请包备份。';renderAll()});
+modal.addEventListener('input',function(e){if(e.target.matches('[data-field],[data-freeform],[data-day-title]'))persistDraft()});modal.addEventListener('change',function(e){if(e.target.matches('input,select,textarea'))persistDraft()});window.addEventListener('pagehide',persistDraft);
+
+function renderDays(){var w=$('.custom-days',modal);w.innerHTML=draft.map(function(d,i){return'<button class="'+(i===selectedDay?'is-active':'')+'" data-day="'+i+'" aria-label="第 '+d.day+' 天，'+esc(theme(d))+'"><b>'+String(d.day).padStart(2,'0')+'</b><span><strong>'+esc(shortDate(d))+'</strong><small>'+esc(theme(d))+'</small></span></button>'}).join('');$$('[data-day]',w).forEach(function(b){b.onclick=function(){selectedDay=+b.dataset.day;renderAll();persistDraft()}})}
 function timeOptions(raw){var selected=timeValue(raw),values=[];for(var m=360;m<1440;m+=15)values.push(String(Math.floor(m/60)).padStart(2,'0')+':'+String(m%60).padStart(2,'0'));if(selected&&values.indexOf(selected)<0)values.push(selected);values.sort();return'<option value="">待定</option>'+values.map(function(v){return'<option value="'+v+'"'+(v===selected?' selected':'')+'>'+v+'</option>'}).join('')}
 function card(p){return'<article draggable="false" data-place="'+p.id+'"><div class="custom-row-main"><button class="custom-drag" aria-label="按住拖动排序">⋮⋮</button><time data-open-field="arrival" tabindex="0" role="button" aria-label="修改抵达时间">'+esc(timeValue(p.arrival)||'待定')+'</time><span><b data-open-field="name" tabindex="0" role="button">'+esc(p.name||'由 Agent 推荐')+'</b><small data-open-field="dwell" tabindex="0" role="button">'+esc(p.dwell||'停留时间由 Agent 建议')+'</small></span><button class="custom-delete" data-delete="'+p.id+'" aria-label="删除">×</button></div><div class="custom-row-editor" hidden><label><span>抵达时间（每15分钟）</span><select data-field="arrival">'+timeOptions(p.arrival)+'</select></label><label><span>地点名称</span><input data-field="name" value="'+esc(p.name)+'" placeholder="由 Agent 推荐"></label><label><span>逗留时间</span><input data-field="dwell" value="'+esc(p.dwell)+'" placeholder="由 Agent 建议"></label></div></article>'}
 function gap(after,before){var t=after&&before?midTime(after,before):after?timeValue(after.arrival):before?timeValue(before.arrival):'12:30';return'<div class="custom-gap"><button data-inline="'+(after?after.id:'start')+'" data-inline-time="'+t+'" aria-label="在此处增加地点">＋</button></div>'}function midTime(a,b){var m=Math.round((clock(a.arrival)+clock(b.arrival))/2);return String(Math.floor(m/60)).padStart(2,'0')+':'+String(m%60).padStart(2,'0')}
@@ -13,19 +20,19 @@
 function syncTheme(index,value){var sourceDay=$$('#route .day')[index];if(!sourceDay)return;var sourceTitle=$('summary b',sourceDay),sourceMeta=$('summary small',sourceDay);if(sourceTitle)sourceTitle.textContent=value;if(sourceMeta){var parts=sourceMeta.textContent.split('·');parts[parts.length-1]=' '+value;sourceMeta.textContent=parts.join('·')}}
 function renderTimeline(){var d=draft[selectedDay],g={morning:[],afternoon:[],evening:[]};d.places.forEach(function(p){g[period(p)].push(p)});var nextAfterMorning=g.afternoon[0]||g.evening[0]||null,nextAfterAfternoon=g.evening[0]||null;$('.custom-timeline',modal).innerHTML=groupHtml('morning','上午','08:00—12:00',g.morning,nextAfterMorning)+groupHtml('afternoon','下午','12:00—18:00',g.afternoon,nextAfterAfternoon)+groupHtml('evening','晚上','18:00 以后',g.evening,null);var head=$('.custom-existing>header',modal),titleInput=$('[data-day-title]',head);if(!titleInput){var label=document.createElement('label');label.className='custom-day-title';label.innerHTML='<span>当日主题</span><input data-day-title maxlength="12" aria-label="修改当日主题">';head.insertBefore(label,$('[data-restore]',head));titleInput=$('[data-day-title]',head)}titleInput.value=d.title||'';titleInput.oninput=function(){var value=titleInput.value.trim()||('第 '+d.day+' 天');d.title=value;syncTheme(selectedDay,value);upsertOp({id:'title-'+d.day,type:'title',day:d.day,placeId:'day-title',sentence:'请将第 '+d.day+' 天的当日主题改为“'+value+'”。'});renderDays();renderOps();status.textContent='当日主题已修改。'};$$('[data-delete]',modal).forEach(function(b){b.onclick=function(){removePlace(b.dataset.delete)}});function openField(target){var article=target.closest('[data-place]'),field=target.dataset.openField,editor=$('.custom-row-editor',article),input=$('[data-field="'+field+'"]',editor);$$('.custom-row-editor',modal).forEach(function(x){if(x!==editor)x.hidden=true});editor.hidden=false;requestAnimationFrame(function(){input&&input.focus()})}$$('[data-open-field]',modal).forEach(function(x){x.onclick=function(){openField(x)};x.onkeydown=function(e){if(e.key==='Enter'||e.key===' '){e.preventDefault();openField(x)}}});$$('[data-inline]',modal).forEach(function(b){b.onclick=function(){openInline(b)}});$$('[data-field]',modal).forEach(function(input){var article=input.closest('[data-place]'),id=article.dataset.place;if(input.dataset.field==='arrival'){input.onchange=function(){editPlace(id,'arrival',input.value);d.places.sort(function(a,b){return clock(a.arrival)-clock(b.arrival)});renderAll();status.textContent='抵达时间与行程位置已同步更新。'}}else{input.oninput=function(){var value=input.value,summary=$('.custom-row-main>span',article);if(input.dataset.field==='name')$('b',summary).textContent=value||'由 Agent 推荐';if(input.dataset.field==='dwell')$('small',summary).textContent=value||'停留时间由 Agent 建议';editPlace(id,input.dataset.field,value)}}});enableLiveDrag()}
 function openInline(button){$$('.custom-inline-form',modal).forEach(function(x){x.remove()});var f=document.createElement('div');f.className='custom-inline-form';f.innerHTML='<select><option>景点</option><option>餐厅</option><option>购物或店铺</option><option>瑜伽或健身</option><option>SPA</option><option>其他地点</option></select><input placeholder="地点名称（可留空）"><input type="time" value="'+button.dataset.inlineTime+'"><button data-inline-save>加入</button><button data-inline-cancel aria-label="取消">×</button>';button.parentElement.after(f);$('[data-inline-cancel]',f).onclick=function(){f.remove()};$('[data-inline-save]',f).onclick=function(){addPlace($('select',f).value,$$('input',f)[0].value.trim(),$$('input',f)[1].value,button.dataset.inline);f.remove()};$('input',f).focus()}
-function upsertOp(op){ops=ops.filter(function(x){return !(x.type===op.type&&x.placeId===op.placeId&&x.day===op.day)});ops.push(op)}function editPlace(id,field,value){var d=draft[selectedDay],p=d.places.find(function(x){return x.id===id});if(!p)return;p[field]=field==='arrival'?(value?value+' 左右抵达':''):value;var old=original[selectedDay].places.find(function(x){return x.id===id}),changes=[];if(!old||p.name!==old.name)changes.push('名称改为“'+(p.name||'由 Agent 推荐')+'”');if(!old||timeValue(p.arrival)!==timeValue(old.arrival))changes.push('抵达时间改为 '+(timeValue(p.arrival)||'由 Agent 安排'));if(!old||p.dwell!==old.dwell)changes.push('逗留时间改为“'+(p.dwell||'由 Agent 建议')+'”');upsertOp({id:'edit-'+id,type:'edit',day:d.day,placeId:id,sentence:'请修改第 '+d.day+' 天的“'+(old?old.name:p.name)+'”：'+changes.join('，')+'；同步更新行文与 Mini Route。'});renderDays();renderOps();status.textContent='修改已记录。'}
-function addPlace(kind,name,time,afterId){var d=draft[selectedDay],id='new-'+uid(),p={id:id,name:name||'由 Agent 推荐的'+kind,arrival:time?time+' 左右抵达':'',dwell:'',isNew:true},idx=afterId==='start'?0:d.places.findIndex(function(x){return x.id===afterId})+1;if(afterId==null||idx<=0){idx=d.places.findIndex(function(x){return clock(x.arrival)>clock(time)});if(idx<0)idx=d.places.length}d.places.splice(idx,0,p);upsertOp({id:'add-'+id,type:'add',day:d.day,placeId:id,sentence:'请在第 '+d.day+' 天'+(time?' '+time+' 左右':'合适时间')+'增加'+kind+'“'+(name||'请结合路线推荐')+'”；按当前顺序安排，补齐交通、停留及对应介绍。'});renderAll();status.textContent='新地点已加入当日草稿。'}
-function removePlace(id){var d=draft[selectedDay],i=d.places.findIndex(function(p){return p.id===id});if(i<0)return;var p=d.places.splice(i,1)[0];ops=ops.filter(function(o){return !(o.type==='add'&&o.placeId===id)});if(!p.isNew)upsertOp({id:'delete-'+id,type:'delete',day:d.day,placeId:id,sentence:'请删除第 '+d.day+' 天的“'+p.name+'”，同步整理行文、Mini Route 与相关介绍。'});renderAll()}
+function upsertOp(op){ops=ops.filter(function(x){return !(x.type===op.type&&x.placeId===op.placeId&&x.day===op.day)});ops.push(op)}function editPlace(id,field,value){var d=draft[selectedDay],p=d.places.find(function(x){return x.id===id});if(!p)return;p[field]=field==='arrival'?(value?value+' 左右抵达':''):value;var old=original[selectedDay].places.find(function(x){return x.id===id}),changes=[];if(!old||p.name!==old.name)changes.push('名称改为“'+(p.name||'由 Agent 推荐')+'”');if(!old||timeValue(p.arrival)!==timeValue(old.arrival))changes.push('抵达时间改为 '+(timeValue(p.arrival)||'由 Agent 安排'));if(!old||p.dwell!==old.dwell)changes.push('逗留时间改为“'+(p.dwell||'由 Agent 建议')+'”');upsertOp({id:'edit-'+id,type:'edit',day:d.day,placeId:id,sentence:'请修改第 '+d.day+' 天的“'+(old?old.name:p.name)+'”：'+changes.join('，')+'；同步更新行文与 Mini Route。'});renderDays();renderOps();status.textContent='修改已记录。';persistDraft()}
+function addPlace(kind,name,time,afterId){var d=draft[selectedDay],id='new-'+uid(),p={id:id,name:name||'由 Agent 推荐的'+kind,arrival:time?time+' 左右抵达':'',dwell:'',isNew:true},idx=afterId==='start'?0:d.places.findIndex(function(x){return x.id===afterId})+1;if(afterId==null||idx<=0){idx=d.places.findIndex(function(x){return clock(x.arrival)>clock(time)});if(idx<0)idx=d.places.length}d.places.splice(idx,0,p);upsertOp({id:'add-'+id,type:'add',day:d.day,placeId:id,sentence:'请在第 '+d.day+' 天'+(time?' '+time+' 左右':'合适时间')+'增加'+kind+'“'+(name||'请结合路线推荐')+'”；按当前顺序安排，补齐交通、停留及对应介绍。'});renderAll();persistDraft();status.textContent='新地点已加入当日草稿。'}
+function removePlace(id){var d=draft[selectedDay],i=d.places.findIndex(function(p){return p.id===id});if(i<0)return;var p=d.places.splice(i,1)[0];ops=ops.filter(function(o){return !(o.type==='add'&&o.placeId===id)});if(!p.isNew)upsertOp({id:'delete-'+id,type:'delete',day:d.day,placeId:id,sentence:'请删除第 '+d.day+' 天的“'+p.name+'”，同步整理行文、Mini Route 与相关介绍。'});renderAll();persistDraft()}
 function flip(container,mutate){var first=new Map();$$('[data-place]',container).forEach(function(x){first.set(x,x.getBoundingClientRect())});mutate();$$('[data-place]',container).forEach(function(x){var a=first.get(x),b=x.getBoundingClientRect();if(!a)return;x.animate([{transform:'translate('+(a.left-b.left)+'px,'+(a.top-b.top)+'px)'},{transform:'translate(0,0)'}],{duration:190,easing:'cubic-bezier(.2,.8,.2,1)'})})}
 function enableLiveDrag(){var active=null,ghost=null,pointerId=null,moved=false,container=$('.custom-timeline',modal),panel=$('.itinerary-customizer__panel',modal);function clearSelection(){var s=window.getSelection&&window.getSelection();if(s&&s.removeAllRanges)s.removeAllRanges()}function moveOver(card,y){if(!active||card===active)return;moved=true;flip(container,function(){card.parentElement.insertBefore(active,y>card.getBoundingClientRect().top+card.offsetHeight/2?card.nextSibling:card)})}function removeGlobal(){window.removeEventListener('pointermove',touchMove);window.removeEventListener('pointerup',touchEnd);window.removeEventListener('pointercancel',touchCancel);window.removeEventListener('blur',touchCancel);window.removeEventListener('pagehide',touchCancel);document.removeEventListener('visibilitychange',visibilityCancel)}function cleanup(commit){var id=active&&active.dataset.place;removeGlobal();$$('.custom-drag-ghost').forEach(function(x){x.remove()});$$('.is-dragging',modal).forEach(function(x){x.classList.remove('is-dragging')});$$('[data-place]',container).forEach(function(x){x.draggable=false});document.body.classList.remove('custom-drag-active');document.documentElement.classList.remove('custom-drag-active');active=null;ghost=null;pointerId=null;clearSelection();if(commit&&id&&moved)syncOrder(id);moved=false}function touchMove(e){if(!active||e.pointerId!==pointerId)return;e.preventDefault();clearSelection();if(ghost)ghost.style.transform='translate3d('+(e.clientX-36)+'px,'+(e.clientY-34)+'px,0)';var all=$$('[data-place]',container).filter(function(x){return x!==active}),over=all.find(function(x){var r=x.getBoundingClientRect();return e.clientY>=r.top&&e.clientY<=r.bottom});if(over)moveOver(over,e.clientY);var pr=panel.getBoundingClientRect();if(e.clientY<pr.top+82)panel.scrollTop-=10;else if(e.clientY>pr.bottom-82)panel.scrollTop+=10}function touchEnd(e){if(pointerId!==null&&e.pointerId!==pointerId)return;cleanup(true)}function touchCancel(){cleanup(false)}function visibilityCancel(){if(document.hidden)cleanup(false)}modal._finishDrag=touchCancel;$$('[data-place]',container).forEach(function(card){var h=$('.custom-drag',card);card.draggable=false;h.onmousedown=function(e){if(e.button!==0)return;card.draggable=true;active=card;moved=false};card.ondragstart=function(e){if(!card.draggable){e.preventDefault();return}active=card;moved=false;card.classList.add('is-dragging');e.dataTransfer.effectAllowed='move';e.dataTransfer.setData('text/plain','')};card.ondragover=function(e){e.preventDefault();moveOver(card,e.clientY)};card.ondragend=function(){cleanup(true)};h.onpointerdown=function(e){if(e.pointerType==='mouse')return;e.preventDefault();e.stopPropagation();cleanup(false);active=card;pointerId=e.pointerId;moved=false;clearSelection();card.classList.add('is-dragging');document.body.classList.add('custom-drag-active');document.documentElement.classList.add('custom-drag-active');ghost=card.cloneNode(true);ghost.className='custom-drag-ghost';ghost.draggable=false;ghost.setAttribute('aria-hidden','true');ghost.style.width=Math.min(card.getBoundingClientRect().width,360)+'px';document.body.appendChild(ghost);ghost.style.transform='translate3d('+(e.clientX-36)+'px,'+(e.clientY-34)+'px,0)';window.addEventListener('pointermove',touchMove,{passive:false});window.addEventListener('pointerup',touchEnd,{passive:false});window.addEventListener('pointercancel',touchCancel,{passive:false});window.addEventListener('blur',touchCancel);window.addEventListener('pagehide',touchCancel);document.addEventListener('visibilitychange',visibilityCancel)}})}
 function minutesToTime(m){m=Math.max(420,Math.min(1320,Math.round(m/5)*5));return String(Math.floor(m/60)).padStart(2,'0')+':'+String(m%60).padStart(2,'0')}
 function suggestMovedTime(places,movedId){var i=places.findIndex(function(p){return p.id===movedId});if(i<0)return'';var prev=places[i-1],next=places[i+1],suggested;if(prev&&next){suggested=(clock(prev.arrival)+clock(next.arrival))/2}else if(prev){suggested=clock(prev.arrival)+90}else if(next){suggested=clock(next.arrival)-90}else{suggested=600}var value=minutesToTime(suggested);places[i].arrival=value+' 左右抵达';return value}
-function syncOrder(movedId){var ids=$$('.custom-timeline [data-place]',modal).map(function(x){return x.dataset.place}),d=draft[selectedDay],map={};d.places.forEach(function(p){map[p.id]=p});d.places=ids.map(function(id){return map[id]}).filter(Boolean);var suggested=suggestMovedTime(d.places,movedId);upsertOp({id:'reorder-'+d.day,type:'reorder',day:d.day,sentence:'请按以下顺序调整第 '+d.day+' 天：'+d.places.map(function(p){return p.name}).join(' → ')+(suggested?'；将“'+(map[movedId]?map[movedId].name:'移动地点')+'”的抵达时间暂调为 '+suggested:'')+'；同步重算交通与其余时间。'});renderAll();status.textContent=suggested?'顺序已更新，并自动建议新的抵达时间。':'新顺序已记录。'}
-function appendPhotoRef(dayIndex,ref){var day=$$('#route .day')[dayIndex],note=day&&$('.photo-note',day);if(!note)return;var board=$('[data-photo-board]',note);if(!board){board=document.createElement('div');board.className='photo-shotboard';board.setAttribute('data-photo-board','');board.innerHTML='<div class="photo-shotboard-grid"></div>';note.appendChild(board)}var grid=$('.photo-shotboard-grid',board),card=document.createElement('article');card.className='photo-sample-card';card.setAttribute('data-customizer-ref',ref.id);card.innerHTML='<figure><img src="'+ref.src+'" alt="用户上传的参考样片"><span>自选参考</span></figure><div><b>参考样片</b><p>可交给 Agent 补充机位、构图与拍摄建议。</p></div>';grid.appendChild(card)}
-function renderPhotoRefs(){var list=photoRefs[selectedDay]||[],w=$('.custom-photo-preview',modal);w.innerHTML=list.length?list.map(function(ref){return '<figure><img src="'+ref.src+'" alt="参考样片"><figcaption>'+esc(ref.name||'参考样片')+'</figcaption><button data-remove-photo="'+ref.id+'" aria-label="移除图片">×</button></figure>'}).join(''):'<p>尚未上传；可以选择多张照片。</p>';$$('[data-remove-photo]',w).forEach(function(b){b.onclick=function(){photoRefs[selectedDay]=(photoRefs[selectedDay]||[]).filter(function(ref){return ref.id!==b.dataset.removePhoto});var card=document.querySelector('[data-customizer-ref="'+b.dataset.removePhoto+'"]');if(card)card.remove();renderPhotoRefs()}})}
-function addPhotoFiles(files){Array.from(files||[]).slice(0,6).forEach(function(file){var reader=new FileReader();reader.onload=function(){var ref={id:'photo-'+uid(),name:file.name,src:reader.result};(photoRefs[selectedDay]||(photoRefs[selectedDay]=[])).push(ref);appendPhotoRef(selectedDay,ref);renderPhotoRefs()};reader.readAsDataURL(file)})}function renderOps(){var w=$('.custom-request-list',modal);$('.custom-request header em',modal).textContent=ops.length+' 项';w.innerHTML=ops.length?ops.map(function(o,i){return'<article><span>'+String(i+1).padStart(2,'0')+'</span><p>'+esc(o.sentence)+'</p><button data-undo="'+o.id+'" aria-label="撤销">×</button></article>'}).join(''):'<p class="custom-empty">尚未修改行程。</p>';$$('[data-undo]',w).forEach(function(b){b.onclick=function(){undo(b.dataset.undo)}})}function undo(id){var op=ops.find(function(o){return o.id===id});if(!op)return;ops=ops.filter(function(o){return o.id!==id});var di=draft.findIndex(function(d){return d.day===op.day});if(op.type==='delete'||op.type==='edit'){var src=original[di].places.find(function(p){return p.id===op.placeId}),i=draft[di].places.findIndex(function(p){return p.id===op.placeId});if(src){if(i<0)draft[di].places.splice(Math.min(src.order-1,draft[di].places.length),0,copy(src));else draft[di].places[i]=copy(src)}}if(op.type==='add')draft[di].places=draft[di].places.filter(function(p){return p.id!==op.placeId});if(op.type==='reorder'){var news=draft[di].places.filter(function(p){return p.isNew});draft[di].places=copy(original[di].places).filter(function(p){return !ops.some(function(x){return x.type==='delete'&&x.placeId===p.id})}).concat(news)}renderAll();status.textContent='已撤销，行程同步恢复。'}
-function restoreDay(){var day=draft[selectedDay].day;draft[selectedDay]=copy(original[selectedDay]);syncTheme(selectedDay,draft[selectedDay].title);ops=ops.filter(function(o){return o.day!==day});renderAll();status.textContent='当天行程与申请已全部还原。'}function renderAll(){renderDays();renderTimeline();renderPhotoRefs();renderOps()}
-function open(){original=sourceDays();if(!ops.length)draft=copy(original);renderAll();modal.hidden=false;requestAnimationFrame(function(){modal.classList.add('is-open')});document.body.style.overflow='hidden'}function close(){if(modal._finishDrag)modal._finishDrag();modal.classList.remove('is-open');document.body.style.overflow='';setTimeout(function(){modal.hidden=true},190)}window.BaliCustomizer={open:open};launch.onclick=open;$('.itinerary-customizer__close',modal).onclick=close;modal.onclick=function(e){if(e.target===modal)close()};$('[data-restore]',modal).onclick=restoreDay;$('[data-photo-input]',modal).onchange=function(){addPhotoFiles(this.files);this.value=''};$('[data-add]',modal).onclick=function(){addPlace($('[data-kind]',modal).value,$('[data-name]',modal).value.trim(),$('[data-time]',modal).value,null);$('[data-name]',modal).value='';$('[data-time]',modal).value=''};$('[data-copy]',modal).onclick=function(){var note=$('[data-freeform]',modal).value.trim();if(!ops.length&&!note){status.textContent='当前没有修改或建议。';return}copyText(prompt(ops,original,note)).then(function(){status.textContent='完整申请已复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'}).catch(function(){status.textContent='复制未成功，请选择修改申请文字手动复制。'})};$('[data-download]',modal).onclick=function(){var note=$('[data-freeform]',modal).value.trim();if(!ops.length&&!note){status.textContent='当前没有修改或建议。';return}download(payload(ops,original,note,photoRefs));status.textContent='完整申请包已下载，包含参考样片。'}
+function syncOrder(movedId){var ids=$$('.custom-timeline [data-place]',modal).map(function(x){return x.dataset.place}),d=draft[selectedDay],map={};d.places.forEach(function(p){map[p.id]=p});d.places=ids.map(function(id){return map[id]}).filter(Boolean);var suggested=suggestMovedTime(d.places,movedId);upsertOp({id:'reorder-'+d.day,type:'reorder',day:d.day,sentence:'请按以下顺序调整第 '+d.day+' 天：'+d.places.map(function(p){return p.name}).join(' → ')+(suggested?'；将“'+(map[movedId]?map[movedId].name:'移动地点')+'”的抵达时间暂调为 '+suggested:'')+'；同步重算交通与其余时间。'});renderAll();persistDraft();status.textContent=suggested?'顺序已更新，并自动建议新的抵达时间。':'新顺序已记录。'}
+function appendPhotoRef(dayIndex,ref){if(document.querySelector('[data-customizer-ref="'+ref.id+'"]'))return;var day=$$('#route .day')[dayIndex],note=day&&$('.photo-note',day);if(!note)return;var board=$('[data-photo-board]',note);if(!board){board=document.createElement('div');board.className='photo-shotboard';board.setAttribute('data-photo-board','');board.innerHTML='<div class="photo-shotboard-grid"></div>';note.appendChild(board)}var grid=$('.photo-shotboard-grid',board),card=document.createElement('article');card.className='photo-sample-card';card.setAttribute('data-customizer-ref',ref.id);card.innerHTML='<figure><img src="'+ref.src+'" alt="用户上传的参考样片"><span>自选参考</span></figure><div><b>参考样片</b><p>可交给 Agent 补充机位、构图与拍摄建议。</p></div>';grid.appendChild(card)}
+function renderPhotoRefs(){var list=photoRefs[selectedDay]||[],w=$('.custom-photo-preview',modal);w.innerHTML=list.length?list.map(function(ref){return '<figure><img src="'+ref.src+'" alt="参考样片"><figcaption>'+esc(ref.name||'参考样片')+'</figcaption><button data-remove-photo="'+ref.id+'" aria-label="移除图片">×</button></figure>'}).join(''):'<p>尚未上传；可以选择多张照片。</p>';$$('[data-remove-photo]',w).forEach(function(b){b.onclick=function(){photoRefs[selectedDay]=(photoRefs[selectedDay]||[]).filter(function(ref){return ref.id!==b.dataset.removePhoto});var card=document.querySelector('[data-customizer-ref="'+b.dataset.removePhoto+'"]');if(card)card.remove();renderPhotoRefs();persistDraft()}})}
+function addPhotoFiles(files){var dayIndex=selectedDay;Array.from(files||[]).filter(function(file){return /^image\//.test(file.type)}).slice(0,6).forEach(function(file){if(file.size>20*1024*1024){saveStatus.textContent='图片未保存：单张请控制在20MB以内。';return}var reader=new FileReader();reader.onerror=function(){saveStatus.textContent='图片读取失败，其他草稿仍保留。'};reader.onload=function(){var ref={id:'photo-'+uid(),name:file.name,src:reader.result};(photoRefs[dayIndex]||(photoRefs[dayIndex]=[])).push(ref);appendPhotoRef(dayIndex,ref);renderPhotoRefs();persistDraft()};reader.readAsDataURL(file)})}function renderOps(){var w=$('.custom-request-list',modal);$('.custom-request header em',modal).textContent=ops.length+' 项';w.innerHTML=ops.length?ops.map(function(o,i){return'<article><span>'+String(i+1).padStart(2,'0')+'</span><p>'+esc(o.sentence)+'</p><button data-undo="'+o.id+'" aria-label="撤销">×</button></article>'}).join(''):'<p class="custom-empty">尚未修改行程。</p>';$$('[data-undo]',w).forEach(function(b){b.onclick=function(){undo(b.dataset.undo)}})}function undo(id){var op=ops.find(function(o){return o.id===id});if(!op)return;ops=ops.filter(function(o){return o.id!==id});var di=draft.findIndex(function(d){return d.day===op.day});if(op.type==='delete'||op.type==='edit'){var src=original[di].places.find(function(p){return p.id===op.placeId}),i=draft[di].places.findIndex(function(p){return p.id===op.placeId});if(src){if(i<0)draft[di].places.splice(Math.min(src.order-1,draft[di].places.length),0,copy(src));else draft[di].places[i]=copy(src)}}if(op.type==='add')draft[di].places=draft[di].places.filter(function(p){return p.id!==op.placeId});if(op.type==='reorder'){var news=draft[di].places.filter(function(p){return p.isNew});draft[di].places=copy(original[di].places).filter(function(p){return !ops.some(function(x){return x.type==='delete'&&x.placeId===p.id})}).concat(news)}renderAll();persistDraft();status.textContent='已撤销，行程同步恢复。'}
+function restoreDay(){var day=draft[selectedDay].day;draft[selectedDay]=copy(original[selectedDay]);syncTheme(selectedDay,draft[selectedDay].title);ops=ops.filter(function(o){return o.day!==day});renderAll();persistDraft();status.textContent='当天行程与申请已全部还原。'}function renderAll(){renderDays();renderTimeline();renderPhotoRefs();renderOps()}
+function open(){if(!storageReady){restored.then(open);return}renderAll();modal.hidden=false;requestAnimationFrame(function(){modal.classList.add('is-open')});document.body.style.overflow='hidden'}function close(){persistDraft();if(modal._finishDrag)modal._finishDrag();modal.classList.remove('is-open');document.body.style.overflow='';setTimeout(function(){modal.hidden=true},190)}window.BaliCustomizer={open:open};launch.onclick=open;$('.itinerary-customizer__close',modal).onclick=close;modal.onclick=function(e){if(e.target===modal)close()};$('[data-restore]',modal).onclick=restoreDay;$('[data-photo-input]',modal).onchange=function(){addPhotoFiles(this.files);this.value=''};$('[data-add]',modal).onclick=function(){addPlace($('[data-kind]',modal).value,$('[data-name]',modal).value.trim(),$('[data-time]',modal).value,null);$('[data-name]',modal).value='';$('[data-time]',modal).value=''};$('[data-copy]',modal).onclick=function(){var note=$('[data-freeform]',modal).value.trim();if(!ops.length&&!note){status.textContent='当前没有修改或建议。';return}copyText(prompt(ops,original,note)).then(function(copied){status.textContent=copied?'完整申请已复制。':'已显示完整申请文字，请全选后使用系统复制。'})};$('[data-download]',modal).onclick=function(){var note=$('[data-freeform]',modal).value.trim();if(!ops.length&&!note){status.textContent='当前没有修改或建议。';return}download(payload(ops,original,note,photoRefs));status.textContent='完整申请包已准备，包含参考样片；请选择保存文件或系统分享。'}
 function attachTripEntry(){document.addEventListener('click',function(e){if(!e.target.closest('.trip-mode-launch,.trip-mode-day'))return;setTimeout(function(){var a=$('.trip-mode-actions');if(!a||$('[data-customize-trip]',a))return;var b=document.createElement('button');b.dataset.customizeTrip='';b.textContent='调整这趟行程';b.onclick=function(){$('.trip-mode-close')?.click();setTimeout(open,80)};a.prepend(b)},120)})}attachTripEntry()}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();
 
--- a/checklist-memory.js
+++ b/checklist-memory.js
@@ -70,7 +70,9 @@
     inputs.forEach(function (input, index) {
       state[itemKey(input, index)] = input.checked;
     });
-    writeState(state);
+    var saved = writeState(state);
+    var message = root.querySelector('.checklist-memory-status');
+    if (message) message.textContent = saved ? '勾选进度已保存在本机' : '本机保存失败；当前勾选仅留在本次页面，请勿关闭或刷新。';
     refreshVisuals();
   }
 
@@ -86,7 +88,7 @@
   if (heading && !root.querySelector('.checklist-memory-status')) {
     var status = document.createElement('p');
     status.className = 'checklist-memory-status';
-    status.textContent = '勾选进度会自动保存在这台设备上';
+    status.textContent = '勾选后保存到本机；请留意保存结果。';
     heading.insertAdjacentElement('afterend', status);
   }
   restore();
--- a/inline-1.js
+++ b/inline-1.js
@@ -1,7 +1,7 @@
 
 (function(){document.addEventListener('click',function(event){var summary=event.target.closest&&event.target.closest('details > summary'),details=summary&&summary.parentElement;if(!summary||!details||!details.open)return;var screenTop=summary.getBoundingClientRect().top,documentTop=details.getBoundingClientRect().top+window.scrollY;requestAnimationFrame(function(){requestAnimationFrame(function(){if(details.open)return;window.scrollTo({top:Math.max(0,documentTop-screenTop),behavior:'auto'})})})},true)})();
 document.querySelectorAll('.gallery-wrap').forEach(function(gallery){var track=gallery.querySelector('.photo-strip'),slides=track.querySelectorAll('figure'),count=gallery.querySelector('.gallery-count'),active=0;function go(next){active=(next+slides.length)%slides.length;track.scrollTo({left:track.clientWidth*active,behavior:'smooth'});if(count)count.textContent=String(active+1).padStart(2,'0')+' / '+String(slides.length).padStart(2,'0')}var prev=gallery.querySelector('.prev'),next=gallery.querySelector('.next');if(prev)prev.addEventListener('click',function(){go(active-1)});if(next)next.addEventListener('click',function(){go(active+1)});track.addEventListener('scroll',function(){if(track.clientWidth){active=Math.round(track.scrollLeft/track.clientWidth);if(count)count.textContent=String(active+1).padStart(2,'0')+' / '+String(slides.length).padStart(2,'0')}})});
-document.querySelectorAll('.packing-checklist input').forEach(function(box,index){var saved=[];try{saved=JSON.parse(localStorage.getItem('@@NAMESPACE@@-packing-2026')||'[]')}catch(e){}box.checked=saved.indexOf(index)>-1;box.addEventListener('change',function(){var next=[];document.querySelectorAll('.packing-checklist input').forEach(function(input,i){if(input.checked)next.push(i)});localStorage.setItem('@@NAMESPACE@@-packing-2026',JSON.stringify(next));updatePacking()})});function updatePacking(){var boxes=document.querySelectorAll('.packing-checklist input'),done=document.querySelectorAll('.packing-checklist input:checked').length,bar=document.querySelector('.packing-progress span'),label=document.querySelector('.packing-progress b');boxes.forEach(function(input){input.closest('li').classList.toggle('is-packed',input.checked)});if(bar&&boxes.length)bar.style.width=(done/boxes.length*100)+'%';if(label)label.textContent=done+' / '+boxes.length+' 已准备'}updatePacking();
-document.querySelectorAll('.booking-checklist input').forEach(function(box,index){var saved=[];try{saved=JSON.parse(localStorage.getItem('@@NAMESPACE@@-booking-2026')||'[]')}catch(e){}box.checked=saved.indexOf(index)>-1;box.addEventListener('change',function(){var next=[];document.querySelectorAll('.booking-checklist input').forEach(function(input,i){if(input.checked)next.push(i)});localStorage.setItem('@@NAMESPACE@@-booking-2026',JSON.stringify(next));updateBooking()})});function updateBooking(){var boxes=document.querySelectorAll('.booking-checklist input'),done=document.querySelectorAll('.booking-checklist input:checked').length,bar=document.querySelector('.booking-progress span'),label=document.querySelector('.booking-progress b');boxes.forEach(function(input){var card=input.closest('article'),state=input.parentElement.querySelector('span');if(card)card.classList.toggle('is-checked',input.checked);if(state)state.textContent=input.checked?'已确认':'待确认'});if(bar&&boxes.length)bar.style.width=(done/boxes.length*100)+'%';if(label)label.textContent=done+' / '+boxes.length+' 已确认'}updateBooking();
+document.querySelectorAll('.packing-checklist input').forEach(function(box,index){var saved=[];try{saved=JSON.parse(localStorage.getItem('@@NAMESPACE@@-packing-2026')||'[]')}catch(e){}box.checked=saved.indexOf(index)>-1;box.addEventListener('change',function(){var next=[];document.querySelectorAll('.packing-checklist input').forEach(function(input,i){if(input.checked)next.push(i)});try{localStorage.setItem('@@NAMESPACE@@-packing-2026',JSON.stringify(next))}catch(e){box.dataset.saveFailed='true'}updatePacking()})});function updatePacking(){var boxes=document.querySelectorAll('.packing-checklist input'),done=document.querySelectorAll('.packing-checklist input:checked').length,bar=document.querySelector('.packing-progress span'),label=document.querySelector('.packing-progress b');boxes.forEach(function(input){input.closest('li').classList.toggle('is-packed',input.checked)});if(bar&&boxes.length)bar.style.width=(done/boxes.length*100)+'%';if(label)label.textContent=done+' / '+boxes.length+' 已准备'}updatePacking();
+document.querySelectorAll('.booking-checklist input').forEach(function(box,index){var saved=[];try{saved=JSON.parse(localStorage.getItem('@@NAMESPACE@@-booking-2026')||'[]')}catch(e){}box.checked=saved.indexOf(index)>-1;box.addEventListener('change',function(){var next=[];document.querySelectorAll('.booking-checklist input').forEach(function(input,i){if(input.checked)next.push(i)});try{localStorage.setItem('@@NAMESPACE@@-booking-2026',JSON.stringify(next))}catch(e){box.dataset.saveFailed='true'}updateBooking()})});function updateBooking(){var boxes=document.querySelectorAll('.booking-checklist input'),done=document.querySelectorAll('.booking-checklist input:checked').length,bar=document.querySelector('.booking-progress span'),label=document.querySelector('.booking-progress b');boxes.forEach(function(input){var card=input.closest('article'),state=input.parentElement.querySelector('span');if(card)card.classList.toggle('is-checked',input.checked);if(state)state.textContent=input.checked?'已确认':'待确认'});if(bar&&boxes.length)bar.style.width=(done/boxes.length*100)+'%';if(label)label.textContent=done+' / '+boxes.length+' 已确认'}updateBooking();
 (function(){var trigger=document.querySelector('.mobile-nav-trigger'),panel=document.querySelector('.mobile-menu-panel'),backdrop=document.querySelector('.mobile-menu-backdrop');if(!trigger||!panel||!backdrop)return;function setMenu(open){trigger.classList.toggle('is-open',open);panel.classList.toggle('is-open',open);backdrop.classList.toggle('is-open',open);trigger.setAttribute('aria-expanded',String(open));panel.setAttribute('aria-hidden',String(!open));trigger.querySelector('i').textContent=open?'×':'＋';panel.querySelectorAll('a').forEach(function(a){a.tabIndex=open?0:-1})}trigger.addEventListener('click',function(){setMenu(trigger.getAttribute('aria-expanded')!=='true')});backdrop.addEventListener('click',function(){setMenu(false)});panel.querySelectorAll('a,header button').forEach(function(el){el.addEventListener('click',function(){setMenu(false)})});document.addEventListener('keydown',function(e){if(e.key==='Escape')setMenu(false)})})();
 if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){document.documentElement.classList.add('motion-ready');var items=document.querySelectorAll('.section-heading,.flight-card,.hotel-card,.journey-block,.movement-card,.restaurant,.nearby-card,.vocab,.phrase-grid>div,.tips-page');items.forEach(function(el,i){el.classList.add('reveal-item');el.style.setProperty('--reveal-delay',Math.min(i%4,3)*55+'ms')});var io=new IntersectionObserver(function(es){es.forEach(function(e){if(e.isIntersecting){e.target.classList.add('is-visible');io.unobserve(e.target)}})},{threshold:.08,rootMargin:'0px 0px -7%'});items.forEach(function(el){io.observe(el)})}